import React from 'react';

const customSelect = ({item}) => {
    const colors = item.colors;
    console.log(colors)
    return (
       <>
        {}
       </>
    )
}

export default customSelect
